services:
  - type: web
    name: chatbot-educativo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run chatbot_app.py --server.port $PORT --server.headless true
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.14